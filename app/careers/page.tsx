import CareersPageClient from "./CareersPageClient";

export const metadata = {
  title: "Careers — Tamada Media",
  description: "Join South India's largest content network. We're building the future of digital media — come build it with us.",
};

export default function CareersPage() {
  return <CareersPageClient />;
}
